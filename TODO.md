# TODO 列表

- [x] 创建 `main.go` 文件，包含一个基本的 Go Web Server 结构。
- [x] 实现一个简单的 HTTP handler 函数，用于响应根路径 `/` 的请求。
- [x] 启动 HTTP 服务器，监听 8089 端口。
- [x] 初始化 Go Modules (`go mod init`)。
- [x] 按照架构规范，修改 API 接口以返回 JSON 格式（包含 code 和 message 字段）。
- [x] 确保错误处理严格返回中文报错信息。