# 这是 eamon-claw 的工作区

## 架构说明

- cmd/dbmonitor/main.go 是项目入口文件，该入口文件主要实现了：实时监控慢查询日志，当被监控慢查询日志有新增内容时会对该内容进行读取并解析；
- 对慢查询 sql 解析完成后会得到 sql 涉及到的数据表和查询条件；
- 通过只读分析数据库统计上述慢查询 sql 涉及到的表和数据量，如单个数据页存储的数据条数、需要读取的数据条数、字段基数等；
- 再获取主数据库的系统负载，如CPU负载、IO负载等；
- 结合上述获取到的数据表统计信息和主库的负载对慢查询进行计算得到全表扫描和二级索引实际代价；
- 将计算结果输入给大模型，提出对当前数据库进行优化的建议；
- 如果没有输出数据表统计信息和主库的负载信息则直接提出偏向索引方面的优化；

## 计算结果格式

```markdown
# LA-CPM 物理代价计算报告
目标数据表: dou_store_order | 触发特征列: order_status (=)
平均单行大小: 5343 字节 | 估算单物理页容纳记录数: 3 条/页
表总行数: 143419 | 预估总数据页: 47806
条件命中行数: 117909 | 数据占比 (Ratio): 82.21%
预估全表扫描代价 (Cost Full): 76489.80
预估索引回表代价 (Cost Index): 141490.80
```

## 禁忌事项

- 严禁在未感知系统OS负载的前提下盲目推荐大表全表扫描；
- 严禁在缺乏数据选择性支持时推荐建立重复或极长的前缀复合索引；
- 强行规定任何高危变更动作，如涉及千万级大表的 DROP INDEX、长期阻塞的 ALTER TABLE必须严格遵守人在回路协议，输出格式化工具调用指令后挂起控制流；
- 不允许删除根目录的任何文件；
- 只给出优化建议，不允许执行实际命令对数据库或者系统进行修改；
- 优化建议不必仅局限于数据库方面的优化，还可以考虑缓存、消息队列等其他优化手段；
- 不要提交代码；
